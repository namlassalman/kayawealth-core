# Local development profile. Do not commit a real .env file.
AURAWEALTH_ENV=DEV
REDIS_URL=redis://127.0.0.1:6379/0

# Optional overrides. Profile defaults apply when these are omitted.
# CACHE_TTL_SECONDS=10
# QUEUE_JOB_TTL_SECONDS=600
